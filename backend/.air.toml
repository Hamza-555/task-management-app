[build]
  cmd = "go build -o ./tmp/server ./cmd/server"
  bin = "./tmp/server"
  include_ext = ["go"]
  exclude_dir = ["tmp", "bin", "migrations"]
  delay = 500

[log]
  time = false

[misc]
  clean_on_exit = true
